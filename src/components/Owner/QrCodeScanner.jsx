import React from 'react';

const QrScanner = () => {
  let blank;

  return (
    <div>
      this is the qr scanner
      <input type="file" accept="image/*" />
    </div>
  );
};

export default QrScanner;
